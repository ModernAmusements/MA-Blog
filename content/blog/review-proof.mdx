---
title: ReviewProof
description: A Python-based web scraping system for collecting medical practitioner data in Bielefeld, Germany using Playwright and SQLite.
date: 2026-05-11
tags: ['python', 'web-scraping', 'playwright', 'medical-data', 'bielefeld', 'data-collection']
---

This project is a Python-based web scraping system designed to collect comprehensive data about medical practitioners and healthcare facilities in Bielefeld, Germany. The scraper uses Playwright (a modern browser automation library) to interact with Google Search and extract business information including ratings, review counts, addresses, and GDPR-related deletion notices.

![Scraper Interface](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2010.35.42.png)

**Key Objectives:**
- Collect data on 500+ doctors, dentists, clinics, and medical practices
- Capture ratings, review counts, addresses, and deleted review notices
- Maintain data quality through strict validation rules
- Build a reusable, maintainable scraper architecture

## Architecture

### System Overview

```mermaid
graph TB
    subgraph "Data Sources"
        GS[Google Search]
        GM[Google Maps]
    end

    subgraph "Scraper Core"
        BP[Browser Playwright]
        HP[HTML Parser]
        RE[Regex Engine]
    end

    subgraph "Data Processing"
        VA[Validator]
        DR[Duplicate Removal]
        CT[Category Tagging]
    end

    subgraph "Output"
        DB[(SQLite DB)]
        CS[CSV Export]
        VP[Visualization]
    end

    GS --> BP
    GM --> BP
    BP --> HP
    HP --> RE
    RE --> VA
    VA --> DR
    DR --> CT
    CT --> DB
    CT --> CS
    CS --> VP
```

### Component Architecture

```mermaid
classDiagram
    class DoctorScraper {
        +init_db()
        +add_or_update()
        +get_stats()
        +export_csv()
        +handle_consent()
        +scrape_doctor()
        +main()
    }

    class Database {
        +establishments table
        +idx_name index
    }

    class Validator {
        +validate_name()
        +validate_rating()
        +check_medical_context()
    }

    class Parser {
        +extract_rating()
        +extract_address()
        +extract_deleted_reviews()
        +parse_page_text()
    }

    DoctorScraper --> Database
    DoctorScraper --> Validator
    DoctorScraper --> Parser
```

## Data Pipeline

### End-to-End Flow

```mermaid
flowchart LR
    A[Search Terms] --> B[Google Search]
    B --> C[Collect Names]
    C --> D[Name Validation]
    D --> E{Valid?}
    E -->|No| F[Discard]
    E -->|Yes| G[Individual Search]
    G --> H[Extract Data]
    H --> I{Data Complete?}
    I -->|No| J[Skip Entry]
    I -->|Yes| K[Save to DB]
    K --> L[Export CSV]
    L --> M[Visualization]
```

### Phase 1: Search & Collection

The scraper uses a multi-term search strategy to maximize coverage:

```python
search_terms = [
    'Arzt Bielefeld',      # General practitioners
    'Zahnarzt Bielefeld',  # Dentists
    'Klinik Bielefeld'     # Clinics and hospitals
]
```

Each term searches 3 pages × 10 results = 30 potential entries per term.

![Search Results](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2010.36.08.png)

### Phase 2: Name Extraction

```mermaid
sequenceDiagram
    participant Browser
    participant Scraper
    participant Validator

    Browser->>Scraper: Load search results
    Scraper->>Browser: inner_text('body')
    Scraper->>Validator: Send extracted lines
    Validator->>Validator: Filter by length (8-70 chars)
    Validator->>Validator: Check doctor keywords
    Validator->>Validator: Reject UI elements
    Validator-->>Scraper: Valid names
```

### Phase 3: Individual Scraping

```mermaid
flowchart TD
    A[For each name] --> B{Search on Google}
    B --> C[Extract page text]
    C --> D[Parse rating]
    D --> E[Parse address]
    E --> F[Check for deleted reviews]
    F --> G{All fields present?}
    G -->|No| H[Skip entry]
    G -->|Yes| I[Save to DB]
```

## Scraping Methodology

### Browser Setup

```python
browser = await p.chromium.launch(
    headless=False,
    args=['--disable-blink-features=AutomationControlled', '--no-sandbox']
)

context = await browser.new_context(
    viewport={'width': 1920, 'height': 1080},
    user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/124.0.0.0 Safari/537.36'
)
```

**Key configurations:**
- `headless=False`: For visual debugging
- `AutomationControlled` flag: Bypass bot detection
- `no-sandbox`: Required for some Linux environments

### Search URL Pattern

```python
search_url = f'https://www.google.com/search?q={term}&tbm=lcl&hl=de-DE&start={page_num * 10}'
```

| Parameter | Value | Purpose |
|-----------|-------|---------|
| `q` | Search term | Google query |
| `tbm=lcl` | local | Local business results |
| `hl=de-DE` | German | German language results |
| `start` | 0, 10, 20 | Pagination |

### Consent Handling

```mermaid
flowchart TD
    A[Page Load] --> B{Cookie banner present?}
    B -->|Yes| C[Look for accept button]
    C --> D{Found "Alle akzeptieren"?}
    D -->|Yes| E[Click button]
    E --> F[Wait 2 seconds]
    D -->|No| G[Try "Akzeptieren"]
    G -->|Found| E
    B -->|No| H[Continue]
```

### Data Extraction Patterns

#### Rating Pattern
```python
# Pattern: "4,5(103)" → rating: 4.5, reviews: 103
rating_match = re.search(r'(\d+[.,]\d+)\s*\(\s*(\d+)\s*\)', page_text)
if rating_match:
    data['rating'] = rating_match.group(1).replace(',', '.')
    data['total_reviews'] = rating_match.group(2)
```

#### Address Pattern
```python
# German postal code pattern: "33602 Bielefeld"
addr_match = re.search(r'\d{5}\s+[\w\s]+(?:Bielefeld|Stadtteil|str\.?|straße)', page_text, re.IGNORECASE)
if addr_match:
    data['address'] = 'Adresse: ' + addr_match.group(0).strip()
```

#### Deleted Reviews (GDPR)
```python
if 'entfernt' in page_text.lower():
    deleted = await page.evaluate('''() => {
        const divs = document.querySelectorAll('div');
        for (let d of divs) {
            let t = d.innerText || '';
            if (t.includes('entfernt') && t.length > 30 && t.length < 200) {
                return t;
            }
        }
        return '';
    }''')
    data['deleted_reviews'] = deleted.strip() if deleted else ''
```

## Data Validation & Cleaning

### Validation Rules

```mermaid
flowchart TD
    A[Raw Name] --> B{Length 8-70?}
    B -->|No| X[Reject]
    B -->|Yes| C{Starts with quote?}
    C -->|Yes| X
    C -->|No| D{Contains doctor keyword?}
    D -->|No| E{Uppercase start + 15+ chars?}
    E -->|No| X
    E -->|Yes| F[Accept]
    D -->|Yes| F
```

### Doctor Keywords (Must Match)

```python
doctor_keywords = [
    'arzt', 'praxis', 'klinik', 'zentrum', 'dr.', 'dr ',
    'med.', 'med ', 'prof.', 'hausarzt', 'zahnarzt',
    'facharzt', 'mvz', 'therapie', 'psycholog', 'physio',
    'heilkunde', 'chiro', 'podo', 'ergo', 'logo',
    'apotheke', 'hospital', 'krankenhaus', 'zahn'
]
```

### Rejection Patterns

```python
skip_patterns = [
    'bewertung', 'öffnet', 'geschlossen', 'anzeige',
    'website', 'telefon', 'google', 'suche', '·',
    'route', 'weiter', 'anmelden', 'nutzung',
    'datenschutz', 'feedback', 'standort', 'hilfe',
    'gesponsert', 'suche etwas', 'bielefeld', 'deutschland'
]
```

### Clean Categories

**Include (Medical):**
- Doctors (Dr., Dr. med., Prof.)
- Practices (Praxis, Gemeinschaftspraxis)
- Clinics (Klinik, Krankenhaus)
- Dentists (Zahnarzt, Zahnärzte)
- Specialists (Facharzt, Centrum)
- Therapy centers

**Exclude (Non-Medical):**
- Pharmacies (Apotheke)
- Gyms/Fitness (Fitness, Gym, McFIT)
- Opticians (Optik, Augenoptik)
- Labs (Labor, Diagnostik)
- Sanatoriums (Sanitätshaus)

![Validation Process](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2010.36.35.png)

## Database Schema

### SQLite Schema

```sql
CREATE TABLE establishments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    rating TEXT,
    total_reviews TEXT,
    deleted_reviews TEXT,
    address TEXT,
    url TEXT,
    category TEXT,
    scrape_date TEXT,
    last_updated TEXT,
    status TEXT DEFAULT 'active'
);

CREATE INDEX idx_name ON establishments(name);
```

### Entity Relationship

```mermaid
erDiagram
    ESTABLISHMENT {
        int id PK
        string name
        string rating
        string total_reviews
        string deleted_reviews
        string address
        string url
        string category
        string scrape_date
        string last_updated
        string status
    }
```

### Field Descriptions

| Field | Type | Description | Example |
|-------|------|-------------|---------|
| id | INTEGER | Primary key | 1 |
| name | TEXT | Business name | "Dr. med. Hans Müller" |
| rating | TEXT | Rating (1.0-5.0) | "4.5" |
| total_reviews | TEXT | Number of reviews | "103" |
| deleted_reviews | TEXT | GDPR deletion notice | "Einige Ergebnisse..." |
| address | TEXT | Full address | "Adresse: Hauptstr. 1, 33602 Bielefeld" |
| url | TEXT | Google Maps URL | "https://www.google.com/maps/..." |
| category | TEXT | Auto-tagged category | "Doctor", "Dentist", "Clinic" |
| scrape_date | TEXT | First scrape date | "2026-05-11" |
| last_updated | TEXT | Last update date | "2026-05-11" |
| status | TEXT | Active/inactive | "active" |

### Category Auto-Tagging

```python
def category_from_name(name):
    name_lower = name.lower()
    if any(k in name_lower for k in ['zahnarzt', 'dentist', 'dent', 'zahn']):
        return 'Dentist'
    if any(k in name_lower for k in ['klinik', 'krankenhaus', 'hospital']):
        return 'Clinic'
    if any(k in name_lower for k in ['augen', 'dermat', 'herz', 'orthop', 'neurolog', 'gynäkolog', 'frauenarzt', 'urolog']):
        return 'Specialist'
    if any(k in name_lower for k in ['praxis', 'medizinisches zentrum', 'mvz']):
        return 'Medical Practice'
    return 'Doctor'
```

## Data Flow Diagrams

### Complete Pipeline

```mermaid
flowchart TB
    subgraph "Input"
        ST[Search Terms]
    end

    subgraph "Phase 1: Collection"
        S1[Search Terms Loop]
        S2[Load Page]
        S3[Accept Cookies]
        S4[Extract Text]
        S5[Filter Names]
        S6[Store Names]
    end

    subgraph "Phase 2: Processing"
        P1[Deduplicate]
        P2[Find New Names]
        P3[Limit Batch]
    end

    subgraph "Phase 3: Scraping"
        R1[For Each Name]
        R2[Search Name]
        R3[Parse Rating]
        R4[Parse Reviews]
        R5[Parse Address]
        R6[Check Deleted]
        R7[Validate]
    end

    subgraph "Phase 4: Storage"
        DB[(SQLite)]
        CSV[CSV Export]
    end

    ST --> S1
    S1 --> S2
    S2 --> S3
    S3 --> S4
    S4 --> S5
    S5 --> S6
    S6 --> P1
    P1 --> P2
    P2 --> P3
    P3 --> R1
    R1 --> R2
    R2 --> R3
    R3 --> R4
    R4 --> R5
    R5 --> R6
    R6 --> R7
    R7 --> DB
    DB --> CSV
```

### Error Handling Flow

```mermaid
flowchart TD
    A[Start] --> B[Load Page]
    B --> C{Success?}
    C -->|No| D[Skip Term]
    C -->|Yes| E[Extract Text]
    E --> F{Text Valid?}
    F -->|No| G[Log Error]
    G --> H[Continue]
    F -->|Yes| I[Filter Names]
    I --> J[Name Count > 0?]
    J -->|No| K[Log Warning]
    K --> L[Next Term]
    J -->|Yes| M[Process Names]
    M --> N[Batch Save]
    N --> O{Every 10?}
    O -->|Yes| P[Export CSV]
    O -->|No| L
    P --> L
```

## Challenges & Solutions

### Challenge 1: Dynamic Google UI

**Problem:** Google frequently changes their HTML structure and CSS class names.

**Solution:** Use text-based extraction instead of CSS selectors:
```python
# Instead of: card = await page.query_selector('.vwVdIc')
# Use: text = await page.inner_text('body')
```

**Before (Selector-based):**
```python
cards = await page.query_selector_all('.vwVdIc')
for card in cards:
    text = await card.inner_text()
```

**After (Text-based):**
```python
text = await page.inner_text('body')
for line in text.split('\n'):
    if 'Dr.' in line or 'Praxis' in line:
        all_names.add(line)
```

### Challenge 2: Consent Cookie Banners

**Problem:** Google shows cookie consent overlay that blocks content.

**Solution:** Automated button clicking with multiple attempts:
```python
async def handle_consent(page):
    for _ in range(3):
        for text in ['Alle akzeptieren', 'Akzeptieren']:
            btn = await page.query_selector(f'button:has-text("{text}")')
            if btn:
                await btn.click()
                await asyncio.sleep(2)
                return True
```

### Challenge 3: Non-Medical Entries

**Problem:** Search results include gyms, pharmacies, opticians.

**Solution:** Strict filtering with keyword validation:
```python
doctor_kw = ['arzt', 'praxis', 'klinik', 'dr.', 'zahn', ...]
non_medical = ['fitness', 'gym', 'optik', 'apotheke', ...]

if not any(k in name.lower() for k in doctor_kw):
    skip_entry()
if any(k in name.lower() for k in non_medical):
    skip_entry()
```

### Challenge 4: Duplicate Entries

**Problem:** Same clinic appears under different names (e.g., "EvKB Haus Gilead I" vs "EvKB Haus Gilead IV").

**Solution:** Database-level dedup with name matching:
```python
existing = conn.execute(
    'SELECT id FROM establishments WHERE name = ?',
    (data['name'],)
).fetchone()

if existing:
    # Update instead of insert
    conn.execute('UPDATE ... WHERE id = ?', ...)

```

### Challenge 5: Missing Rating Data

**Problem:** Some entries don't display ratings in search snippets.

**Solution:** Strict validation - only save entries with complete data:
```python
if not rating or not reviews:
    print('✗ - Skipped (no rating)')
    continue  # Don't save incomplete entries
```

![Scraping Results](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2010.36.56.png)

## Results & Statistics

### Current Dataset

| Metric | Value |
|--------|-------|
| Total Entries | 157 |
| With Ratings | 157 (100%) |
| With Reviews | 157 (100%) |
| With Deleted Reviews | 58 (37%) |
| With Addresses | 16 (10%) |

### Rating Distribution

```mermaid
pie title Rating Distribution
    "4.5 - 5.0 ⭐" : 89
    "4.0 - 4.4 ⭐" : 45
    "3.5 - 3.9 ⭐" : 18
    "Below 3.5" : 5
```

### Category Breakdown

| Category | Count | Percentage |
|----------|-------|------------|
| Doctor | 62 | 39% |
| Dentist | 35 | 22% |
| Clinic | 28 | 18% |
| Medical Practice | 20 | 13% |
| Specialist | 12 | 8% |

### Deleted Reviews Analysis

```mermaid
flowchart TD
    A[Total Entries: 157] --> B{Deleted Reviews?}
    B -->|Yes| C[58 Entries]
    B -->|No| D[99 Entries]
    C --> E{Reason Type}
    E -->|GDPR| F["Einige Ergebnisse wurden entfernt"]
    E -->|Complaint| G["Bewertungen aufgrund von Beschwerden entfernt"]
    E -->|Count| H["11 bis 20 Bewertungen entfernt"]
```

![Results Dashboard](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2011.04.59.png)

## Technical Stack

### Dependencies

```
playwright>=1.40.0
asyncio
sqlite3
csv
re
datetime
```

### File Structure

```
Bielefeld_Scrape/
├── doctor_scraper.py      # Main scraper
├── doctors.db             # SQLite database
├── doctors.csv            # CSV export
├── visualization/         # React visualization
│   ├── public/data/      # CSV for charts
│   └── src/components/   # React components
└── requirements.txt       # Dependencies
```

![Database Structure](/images/review_proof/Bildschirmfoto%202026-05-04%20um%2011.04.40.png)

## Future Enhancements

### Planned Improvements

1. **Parallel Processing**
   - Use multiple browser contexts simultaneously
   - Reduce total scraping time by 50%

2. **Enhanced Validation**
   - Fuzzy name matching for duplicate detection
   - URL-based deduplication as backup

3. **Address Extraction**
   - Improve regex patterns for German addresses
   - Parse multiple address formats

4. **Review Content**
   - Collect actual review text (with consent)
   - Sentiment analysis on reviews

5. **Monitoring**
   - Track changes over time (re-scrape detection)
   - Alert on rating changes

### Potential Extensions

```mermaid
mindmap
  root((Bielefeld Scraper))
    Data Collection
      Multi-city support
      Other data sources
      Real-time updates
    Analysis
      Rating prediction
      Review sentiment
      Competitor analysis
    Visualization
      Interactive maps
      Rating heatmaps
      Trend charts
```

## Appendix: Code Reference

### Main Loop Structure

```python
async def main():
    conn = init_db()
    existing_names = set(r[0] for r in conn.execute('SELECT name FROM establishments').fetchall())

    search_terms = ['Arzt Bielefeld', 'Zahnarzt Bielefeld', 'Klinik Bielefeld']

    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=False)
        page = await browser.new_page()

        all_names = set()

        # Phase 1: Collect names
        for term in search_terms:
            for page_num in range(3):
                await page.goto(f'https://www.google.com/search?q={term}&tbm=lcl&hl=de-DE&start={page_num * 10}')
                await handle_consent(page)
                text = await page.inner_text('body')

                for line in text.split('\n'):
                    line = line.strip()
                    if is_valid_name(line):
                        all_names.add(line)

        # Phase 2: Scrape each
        for name in all_names:
            if name not in existing_names:
                data = await scrape_doctor(page, name)
                if data and data['rating']:
                    add_or_update(conn, data)

        await browser.close()

    export_csv(conn)
```

### CSV Export Function

```python
def export_csv(conn, filepath='doctors.csv'):
    cursor = conn.execute('''
        SELECT name, rating, total_reviews, deleted_reviews,
               address, url, category, scrape_date, last_updated
        FROM establishments
        ORDER BY name
    ''')

    with open(filepath, 'w', newline='', encoding='utf-8') as f:
        writer = csv.writer(f)
        writer.writerow(['name', 'rating', 'total_reviews', 'deleted_reviews',
                        'address', 'url', 'category', 'scrape_date', 'last_updated'])
        for row in cursor:
            writer.writerow(row)
```

---

## Conclusion

This project demonstrates a practical approach to automated medical data collection from web search results. Key learnings include:

1. **Text-based extraction** is more robust than CSS selectors for dynamic web pages
2. **Strict validation** ensures high data quality even if it means fewer entries
3. **Modular design** allows easy maintenance and extension
4. **Automated cleaning** catches bad entries that slip through initial filtering

The scraper successfully collects validated medical practitioner data from Bielefeld, with 100% of entries containing ratings and review counts. The architecture is designed for extensibility to other cities and data sources.

---

*Document Version: 1.0*  
*Last Updated: May 11, 2026*  
*License: MIT*